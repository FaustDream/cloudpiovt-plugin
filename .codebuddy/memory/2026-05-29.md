# 2026-05-29

## 修复前端代码误读到后端代码

- `findModel` 去掉了"任意可见 editor"和"任意 model"全局兜底策略
- 改为严格的 3 层递进：容器内 editor → modeIds 匹配 → 唯一 model
- 避免 `#jsText` 拿到 `#csText` 的 C# model

## 保留一键抓取写入

- 恢复按钮、常量和 `handleH3yunCaptureAllAndWrite`
- 一键抓取时也自动新建 design.md（已存在时不覆盖）

## 控件信息写入附带 design.md

- 新建模板含基本信息、设计思路、开发任务区
- 已存在时保留不覆盖

## 其他

- Monaco 模型发现改为 5 层递进后回退到 3 层（移除危险兜底）
- 控件选择器扩展为同时匹配 .control-container + .layout-control__item
- 移除页面探测和复制上下文
