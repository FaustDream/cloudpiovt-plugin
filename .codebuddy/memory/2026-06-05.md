# 2026-06-05

## 第二轮：更新机制改为 Git 直连
- 替换 update.json 拉取方案为 Native Host `git_sync` 命令
- Native Host（Rust）：新增 `git_sync` 命令 + `compare_versions()`/`parse_manifest_version()` helper；HostResponse 新增 `currentVersion`/`latestVersion`/`updateAvailable`/`synced`
- 自动检查（chrome.alarms）：仅 fetch + 版本比较，不 touch 本地文件
- 手动同步：options.html 新增「同步更新」按钮，点击后 `git reset --hard origin/master`
- 移除 updateManifestUrl UI 字段；options.js 移除 downloadUpdateLink 引用
- Rebuild Rust release binary + copy to .native-host/publish/
- 所有 JS/Rust/JSON 语法验证通过，无 lint 错误

## 第一轮：初始更新功能
- 新增自动更新功能：`update.json` + `lib/update-check.js` + 设置页更新入口 + `chrome.alarms` 后台检查
- `autoCheckUpdates` 默认 `false`，用户开启后每日检查一次；手动检查按钮即时读取 update.json
- 下载地址使用 GitHub 源码 zip（分支/tag），不依赖 GitHub Releases
- 更新 `manifest.json`（alarms 权限 + raw.githubusercontent.com host_permission）、`options.html/js/css`、`background.js`、`lib/config.js`
- 同步README、使用手册第7.3节、architecture.md、changelog-ai.md 记录更新机制
- 无 lint 错误，功能已全部落地
