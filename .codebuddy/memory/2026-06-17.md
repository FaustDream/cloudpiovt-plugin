# 2026-06-17 工作日志

## DESIGN.md 按文件类型分节规范（10:02）

- **需求**：DESIGN.md 写业务逻辑时要根据 Java 文件或氚云文件做好区分，避免所有文件逻辑都在同一个数据里面
- **修改文件**：`lib/workspace-documents.js`
- **改动**：
  - `buildCodeFileLines()`：文件列表按 `.java`/`.cs` 自动分组为「前端文件」和「服务端文件」
  - 新增 `buildDesignBusinessLogicSections(input)`：按平台生成分节的业务逻辑模板
  - `buildWorkspaceDesignContent()`：「## 业务实现逻辑」从扁平列表改为两个独立子节
    - 云枢：`### 前端交互逻辑（JS/HTML/CSS）` + `### 服务端业务逻辑（Java）`（带"每个 Java 类独立成小节"提示）
    - 氚云：`### 前端交互逻辑（JS）` + `### 后端服务逻辑（C#）`
- **测试结果**：3 files / 146 tests 全部通过，无回归

---

## DESIGN.md 增加响应示例与调用实例规范（10:05）

- **需求**：DESIGN.md 文件写响应示例和调用实例时，里面的字段都要有中文注释
- **修改文件**：`lib/workspace-documents.js`
- **改动**：
  - 新增 `buildDesignExampleSections(input)`：按平台生成「调用实例」和「响应示例」子节
  - `buildWorkspaceDesignContent()`：在「业务实现逻辑」后新增 `## 响应示例与调用实例` 章节
    - 包含 JSON 代码块模板，字段行尾标注 `// 中文名称` 格式
    - 包含请求参数表（参数名/类型/必填/说明）和响应字段表（字段编码/类型/中文名称/说明）
    - 云枢：前端 JS → 业务规则 → 服务端 Java
    - 氚云：前端 JS → Ajax → 后端 C#
    - 顶部统一规则：每个响应 JSON 字段须附带中文名称注释
- **测试结果**：3 files / 146 tests 全部通过，无回归

---

## DESIGN.md 移除验证清单（10:15）

- **需求**：DESIGN.md 不要出现验证清单，只做业务逻辑说明
- **修改文件**：`lib/workspace-documents.js`
- **改动**：`buildWorkspaceDesignContent()` 移除 `## 验证清单` 章节（含核对 README、FromCode、边界条件三个 checklist 项）
- **测试结果**：3 files / 146 tests 全部通过，无回归

---

## 版本 1.4.5 → 1.4.6 发布（10:16）

- **触发**：将以上三次 DESIGN.md 规范改动作为小版本更新
- **修改文件**：
  - `manifest.json`: version → "1.4.6"
  - `update.json`: version/title/releaseDate 更新 + 新增 1.4.6 发布说明
  - `lib/release-notes.js`: CURRENT_EXTENSION_VERSION → "1.4.6" + 新增 1.4.6 条目
  - `AGENTS.md`: 版本更新章节新增 1.4.6 更新注释
- **1.4.6 更新内容**：
  1. DESIGN.md 业务逻辑按文件类型（JS/Java/C#）分节
  2. DESIGN.md 新增响应示例与调用实例章节，字段强制中文注释
  3. DESIGN.md 涉及代码文件按前端/服务端自动分组
  4. DESIGN.md 移除验证清单，专注业务逻辑说明

---

## README.md 模板精简 + 版本 1.4.6 → 1.4.7（10:42）

- **需求**：README.md 去掉所有默认值，只保留"需求文档"标题
- **修改文件**：
  - `lib/workspace-documents.js`：`buildWorkspaceReadmeContent()` 精简为只输出 `# 需求文档`
  - `manifest.json`: version → "1.4.7"
  - `update.json`: version/title/releaseDate + 新增 1.4.7 发布说明
  - `lib/release-notes.js`: CURRENT_EXTENSION_VERSION → "1.4.7" + 新增 1.4.7 条目
  - `AGENTS.md`: 版本更新章节新增 1.4.7 更新注释
- **测试结果**：3 files / 146 tests 全部通过

---

## 平台默认目录兜底功能 + 版本 1.4.7 → 1.5.0（14:39）

- **需求**：用户在设置页设置云枢和氚云的默认目录，当目录选择无记忆或出错时自动兜底
- **设计决策（grill-me 逐项确认）**：
  1. 配置存储：`fallbackDirectoryPaths: { cloudpivot, h3yun }`，独立于 `targetDirectoryPaths`
  2. 设置页位置：新增独立区块 `#default-directories`，在应用路径和运行日志之间
  3. 目录控件：文本输入 + 「选择目录」按钮（复用 `pickNativeDirectory`）
  4. 兜底触发：自动透明，不弹确认
  5. 兜底范围：`ensureTargetDirectorySnapshot` + `getStoredTargetDirectorySelection` 两处
  6. 版本号：1.5.0（中等规模新功能）
- **修改文件**：
  - `lib/config.js`：新增 `DEFAULT_CONFIG.fallbackDirectoryPaths`、`getPlatformKeyFromPageType()`、`normalizeFallbackDirectoryPaths()`、`getFallbackDirectoryPathByPlatform()`，`loadConfig`/`saveConfig` 适配
  - `lib/target-directory-state.js`：`getStoredTargetDirectorySelection` 和 `ensureTargetDirectorySnapshot` 末尾新增兜底分支，根据 pageType 推导平台 key 读取兜底路径
  - `options.html`：新增 `#default-directories` 区块（平台标签 + 输入框 + 选择按钮），快速导航新增「默认目录」链接
  - `options.js`：新增 `setActiveDefaultDirTab`、`renderFallbackDirectoryPaths`、`handlePickFallbackDirectory` 函数 + DOM 查询 + 事件绑定，`handleSubmit`/`handleReset` 包含 `fallbackDirectoryPaths`
  - `options.css`：新增 `.default-dir-field` 样式（输入框 + 按钮横向排列）
  - `manifest.json`：version → "1.5.0"
  - `lib/release-notes.js`：CURRENT_EXTENSION_VERSION → "1.5.0" + 新增 1.5.0 条目
  - `update.json`：version/title 更新 + 新增 1.5.0 发布说明
  - `.codebuddy/memory/MEMORY.md`：新增平台默认目录兜底章节
- **验证**：所有修改文件 lint 零错误

---

## Bug 修复：新增打开方式后删除按钮无效 + 版本 1.5.0 → 1.5.1（15:08）

- **问题**：设置页新增自定义打开方式后，点击删除按钮无法删除
- **根因**：`handleDeleteLauncher()` 中 `deleteLauncherIcon()` 在新创建的启动器无图标文件时返回 `{ ok: false }`，抛出错误被 `handleLauncherListClick` 的 `catch` 捕获，导致 `currentLaunchers` 未更新、启动器条目保留
- **修复**：`handleDeleteLauncher()` 中用 try-catch 包裹 `deleteLauncherIcon`，图标清理失败不再阻断删除流程
- **修改文件**：
  - `options.js`：`handleDeleteLauncher()` 图标删除改为 try-catch 容错
  - `manifest.json`：version → "1.5.1"
  - `update.json`：version/title + 新增 1.5.1 发布说明
  - `lib/release-notes.js`：CURRENT_EXTENSION_VERSION → "1.5.1" + 新增 1.5.1 条目
  - `options.html`：current-version 和 overview-version 默认值 → "1.5.1"
- **验证**：lint 零错误

---

## 设置页两栏分离式布局重构 + 版本 1.5.1 → 1.6.0（15:20）

- **需求**：设置页配置和说明两种类型在页面上分开，避免过于散乱，对布局和UI进行升级
- **设计**：两栏 Grid 布局，左宽右窄（1.32:1），右栏 sticky 追随滚动
  - **左栏（配置）**：状态总览、更新设置、应用路径、默认目录、运行日志
  - **右栏（说明）**：控件参考、推荐流程、版本更新（标签切换）
  - 移除横向快速导航栏（section-nav），两栏分离后位置一目了然
- **视觉升级**：
  - 保存按钮增加阴影层次（box-shadow）
  - 卡片 hover 微动效
  - 输入框 focus 增加发光边框
  - 区块圆角统一优化
  - 右栏 sticky 定位，跟随滚动
- **修改文件**：
  - `options.html`：完全重构为两栏布局，配置区集中左栏 form，说明区汇聚右栏 aside
  - `options.css`：新增 `.layout-grid`/`.settings-column`/`.docs-column`/`.column-head`/`.page-head` 等两栏布局样式，所有组件样式保留并微调
  - `options-layout.css`：精简为仅保留 scroll-behavior 和响应式边界调整
  - `options.js`：移除 `sectionNavLinks`/`bindSectionNavigation`/`setActiveNavLink` 等死代码，其余功能无变化
  - `manifest.json`：version → "1.6.0"
  - `lib/release-notes.js`：CURRENT_EXTENSION_VERSION → "1.6.0" + 新增 1.6.0 条目
  - `update.json`：version/title + 新增 1.6.0 发布说明
  - `options.html`：version 标签默认值 → "1.6.0"
- **验证**：lint 零错误（4 文件）

---

## 设置页重构为标签页布局（15:29）

- **问题**：两栏布局右栏说明内容过长，导致视觉失衡，配置为主/说明为辅的原则未体现
- **方案**：改为标签页形式，一个卡片容器内顶栏标签导航切换不同面板
- **标签结构**：
  - 主标签：状态总览 | 更新设置 | 应用路径 | 默认目录 | 运行日志 | 说明中心
  - 说明中心内子标签：控件参考 | 推荐流程 | 版本更新
  - 底部固定保存/恢复按钮
- **修改文件**：
  - `options.html`：从两栏布局重构为标签页，移除 form 包裹，保存按钮改为 button click
  - `options.css`：重写布局，新增 `.main-tabs`/`.main-tab`/`.tab-panel`/`.panel-header`/`.card-footer` 样式
  - `options.js`：新增 `setActiveMainTab`、移除 form submit 为 button click、新增 mainTabButtons/mainPanels 查询、init 中设置默认标签为"状态总览"
- **验证**：lint 零错误（3 文件）

---

## 版本信息合并到状态总览卡片（15:42）

- **问题**：顶部标题栏和状态总览版本卡片同时显示"当前版本"，重复出现
- **修改**：
  - 顶部标题栏移除 `.head-version`，只保留标题和副标题
  - 状态总览"版本"卡片改为 `.version-tile`，合并显示版本号（大字 + `vX.X.X` 格式）
  - 版本卡片增加渐变背景和主题色边框，作为状态总览视觉焦点
- **修改文件**：`options.html`、`options.css`、`options.js`（3 文件）
- **验证**：lint 零错误
