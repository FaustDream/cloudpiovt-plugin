# 全局 Agent 规则

## 1. 硬性环境约束

- 文件编码固定使用 UTF-8，不得修改为 GBK、ANSI 或其他编码。
- PowerShell 固定使用 PowerShell 7，不得使用 Windows PowerShell 5。
- 执行命令前，优先确认当前终端、路径、编码与项目环境。
- 设计到文件是需要使用到路径的，尽量不使用绝对路径，使用相对路径。


---

## 2. 指令优先级

1. 用户在当前会话中的明确要求
2. 当前项目根目录及相关子目录中的 AGENTS.md
3. 项目已有文档、代码风格、目录结构、命名约定
4. 本全局 AGENTS.md

说明：

- 本文件只定义通用工程纪律、环境约束、默认行为与沟通规则。
- 项目 AGENTS.md 负责项目实现规则。
- 若项目规则与本文件冲突，除硬性环境约束外，优先执行项目规则。

---

## 3. 默认工作方式

- 默认采用满足质量要求的最短路径。
- 小任务默认轻量执行。
- skill 仅在与任务高度相关时启用。
- 不做与任务无关的重构。
- 不修改任务范围外文件。
- 先理解项目规则，再执行任务。

---

## 4. 任务分流

### 只读任务

以下任务可直接处理：

- 代码解释
- 架构分析
- 报错定位
- 命名建议
- 文档分析
- 只读排查

要求：

- 结论必须清晰
- 根因必须可追溯
- 不得虚构验证结果

### 实现任务

涉及以下内容时，应提升验证与工程纪律：

- 新功能
- Bug 修复
- 行为变更
- 重构
- API 修改
- 数据处理逻辑修改

---

## 5. 代码与注释规则

### 通用原则

- 保持项目原有结构、风格与命名。
- 不引入项目不存在的依赖。
- 不硬编码密钥、Token、凭证。
- 数据库访问必须参数化。

### 中文注释要求

生成或修改 JS、CSS、Java 等代码时：

必须补充：

- 方法业务用途
- 关键变量含义
- 核心业务逻辑
- 状态流转原因
- 外部调用原因
- 字段/表单/子表对应中文含义

禁止：

- 空洞翻译式注释
- 无意义逐行注释
- 用注释掩盖混乱代码

---

## 6. 验证规则

- 不得虚构执行结果。
- 未验证不得声称“已完成”“可提交”“可合并”。
- 关键验证无法执行时，必须明确原因。
- 修改代码后需检查：
  - 功能影响范围
  - 回归风险
  - 中文注释
  - 文档同步

---

## 7. 安全规则

- 不运行危险删除命令。
- 不执行 git reset 等破坏性命令，除非用户明确要求。
- 不使用非 Git 工具修改 .git。
- 不拼接不可信 shell 命令或 SQL。
- 不终止非当前任务产生的进程。

---

## 8. 沟通与输出

- 默认使用简体中文。
- 技术术语可使用英文。
- 代码标识符保持英文。
- 执行类任务强调：
  - 当前动作
  - 进度
  - 验证结果
- 分析类任务强调：
  - 结论
  - 根因
  - 依据
  - 风险
- 输出时要输出当前回答使用到那些规则或者skills
---


## 9. 版本更新
- 一个小问题就更新一个小版本。版本加0.0.1，写好更新注释。
- 多个问题就更新一个中版本。0.1.0，写好更新注释。
- 一个大问题就更新一个大版本。1.0.0，写好更新注释。
- 其中10个小版本升级成一个中版本，10个中版本升级成一个大版本。
- 每次更新一个中版本或者大版本，原有的小版本或者中版本都开始从0计算。

### 1.4.7 - 2026-06-17
- README.md 模板精简为仅"需求文档"标题，去除所有默认填充内容

### 1.4.6 - 2026-06-17
- DESIGN.md 业务逻辑按文件类型（JS/Java/C#）分节
- DESIGN.md 新增响应示例与调用实例章节，字段强制中文注释
- DESIGN.md 涉及代码文件按前端/服务端自动分组
- DESIGN.md 移除验证清单，专注业务逻辑说明
---

